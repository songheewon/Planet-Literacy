{{!< default}} {{#contentFor 'body_custom_class' }} shop-template{{/contentFor}} 
        <div class="y-product-content__wrap" style="margin-top: -40px;">
            {{#product}}
            <div class="y-product-content {{payment.type}} paid-{{payment_status.is_paid}}">
                {{/product}}
                <div class="y-product-content__main">{{bluedot_product}}</div>
            </div>
        </div>

        <style>
            .bluedot-product-excerpt {
                text-overflow: ellipsis;
                overflow: hidden;
                display: -webkit-box;
                -webkit-line-clamp: 3;
                -webkit-box-orient: vertical;
            }

            .progress {
                width: 90% !important;
            }
        </style>